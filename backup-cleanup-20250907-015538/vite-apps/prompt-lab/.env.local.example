# Example env for apps/prompt-lab (copy to .env.local and fill in values)
# This is an example file only — do NOT commit real secrets.

# App runtime
PORT=3002
NODE_ENV=development

# API endpoints
NEXT_PUBLIC_API_URL=http://localhost:3000

# Optional API keys / adapters
OPENAI_API_KEY=sk_xxxxxxxxxxxxxxxxxxxxx    # optional, if prompt-lab calls OpenAI
LATITUDE_API_KEY=                           # optional Latitude adapter key (if used)
OTHER_API_KEY=                              # placeholder for any other integrations

# Notes:
# - Copy to apps/prompt-lab/.env.local and populate real values for local development.
# - Do NOT commit `.env.local`. See docs/SECRETS_WORKFLOW.md for guidance.