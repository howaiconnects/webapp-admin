# Example env for apps/webapp-admin (copy to .env.local and fill in values)
# This is an example file only — do NOT commit real secrets.

# App runtime
PORT=3001                          # Local app port (override if needed)
NODE_ENV=development

# App & API
NEXT_PUBLIC_API_URL=http://localhost:3000   # API endpoint the frontend will call
NEXTAUTH_URL=http://localhost:3001           # NextAuth callback URL (if using NextAuth)

# Database (can point to root DATABASE_URL or a separate DB)
DATABASE_URL=postgres://user:password@db:5432/howaiconnects

# Optional integrations (placeholders)
AIRTABLE_API_KEY=key_xxxxxxxxxxxxxxxxx
MIRO_CLIENT_ID=miro_client_id
MIRO_CLIENT_SECRET=miro_client_secret

# Notes:
# - Copy to apps/webapp-admin/.env.local and populate real values for local development.
# - Do NOT commit `.env.local`. See docs/SECRETS_WORKFLOW.md for guidance.