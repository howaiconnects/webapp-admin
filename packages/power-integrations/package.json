{
  "name": "@howaiconnects/power-integrations",
  "version": "1.0.0",
  "private": true,
  "description": "Power integrations package for external service adapters (Latitude, Airtable, Miro, etc.)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "airtable": "^0.12.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}